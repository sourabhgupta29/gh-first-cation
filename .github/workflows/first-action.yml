name: First Workflow
on: workflow_dispatch
jobs: 
 first-job:
  runs-on: ubuntu-latest
  steps:
   - name: Print Greeting
     run: |
        echo "hello world"
        echo "hello world1"
   - name: Print Bye
     run: echo "Done -Bye"
